apiVersion: v1
kind: Service
metadata:
  name: {{ include "infoblox-solver.fullname" . }}
  labels:
    {{- include "infoblox-solver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infoblox-solver.selectorLabels" . | nindent 4 }}
