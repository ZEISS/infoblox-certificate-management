apiVersion: v1
kind: Service
metadata:
  name: {{ include "infoblox-solver.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "infoblox-solver.name" . }}
    chart: {{ include "infoblox-solver.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "infoblox-solver.name" . }}
    release: {{ .Release.Name }}