apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "infoblox-solver.fullname" . }}-test-connection"
  labels:
    {{- include "infoblox-solver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "infoblox-solver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
